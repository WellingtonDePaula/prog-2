[project]
name = "restful"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "riad.nassiffe@gmail.com", email = "riad.nassiffe@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "flask-sqlalchemy>=3.1.1",
    "flask-wtf>=1.3.0",
]

[project.scripts]
restful = "restful:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
